# IntentCart requires Python >=3.10 because starter/agent.py uses slotted dataclasses.
# Independently tested with Python 3.12.14 and SQLite 3.53.4 with FTS5 enabled.
# The runtime uses Python's standard library only; there are no pip dependencies.
